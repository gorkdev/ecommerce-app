arb-dir: lib/l10n
template-arb-file: app_en.arb
output-localization-file: app_localizations.dart
output-dir: lib/l10n/generated
# The delegate asserts a localization is always in scope, so the getter can be
# non-nullable and call sites stay clean.
nullable-getter: false
